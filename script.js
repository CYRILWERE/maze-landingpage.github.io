function startGame(level) {
    alert(`Starting game at ${level} level!`);
    // Here, you would redirect to the actual game page or initialize the game with the selected difficulty level.
    // For example, you could use window.location.href = `game.html?level=${level}`;
    // Or initialize the game

